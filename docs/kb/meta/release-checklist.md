---
title: Release checklist
tags: [meta]
aliases: [release-checklist, релиз-чеклист]
updated: 2026-07-10
---

# 🚀 Release checklist — что проверить перед тегом

Автоматика (`make lint`, `make test-engine`, CI-джобы `qemu-smoke`/`qemu-install`) ловит
регрессии в логике и на живом busybox-окружении в QEMU. Этот список — то, что автоматика
принципиально не видит: реальное железо, реальный AWG-сервер, живой браузер.

## Перед тегом — автоматика зелёная

- [ ] `make lint` — shellcheck/POSIX-проверки.
- [ ] `make test-engine` — юниты движка на ucode.
- [ ] CI на `master`: `engine-tests`, `qemu-smoke`, `qemu-install` — зелёные.
- [ ] Если менялся Full-тир (sing-box, протоколы, пороги): локально `make qemu-reality` и
      `make qemu-hysteria` — зелёные. Второй печатает ЗАМЕР веса бинаря: сверить его с
      `min_flash_mb` в `FULL_REQUIREMENTS` и с таблицей в [[hardware-requirements]].
- [ ] `PKG_VERSION` в `package/cheburnet/Makefile` **увеличен** относительно предыдущего тега
      (иначе `apk add` на роутерах с уже установленным пакетом будет no-op — см.
      [[bootstrap#Обновление|bootstrap]]).

## Живой прогон на роутере (раз перед релизом, не на каждый коммит)

Нужен реальный роутер (см. [[hardware-requirements]]). Конфиги просить не у кого — стенд с тремя
протоколами поднимается на арендованном VPS за минуту (`tests/vps/provision-lab.sh`), клиентская
сторона и порядок шагов — [tests/router/RUNBOOK.md](../../../tests/router/RUNBOOK.md).

- [ ] **Bootstrap с нуля.** Однострочник из `README.md` на чистом OpenWrt 25.12+ доходит до
      ссылки веб-мастера без ручного вмешательства.
- [ ] **Мастер end-to-end.** preflight → (если подсети совпали — экран LAN-конфликта) → setup
      (VPN-конфиг, Wi-Fi, пароль, DNS-провайдер) → confirm → installing → статус-панель.
- [ ] **Реальный трафик.** Домен из direct-списка резолвится и идёт напрямую (`ip route get`);
      остальной трафик — через VPN-туннель с живым handshake.
- [ ] **Kill-switch.** `ifdown awg0` (или эквивалент для активного протокола) — непрямой трафик
      дропается, direct продолжает работать.
- [ ] **DNS-фильтрация.** Домен из выбранной категории (реклама/18+) резолвится в `0.0.0.0`
      или NXDOMAIN — см. [[encrypted-dns]].
- [ ] **HOME ⇄ TRAVEL.** Переключение через панель меняет поведение, переживает reboot.
- [ ] **Перезагрузка: split-tunnel жив.** После ребута `ip route get <IP-из-набора-direct> mark 0x1`
      обязан вести МИМО туннеля. Проверять именно направление, а не «адрес попал в набор»: правила
      policy-routing живут только в ядре, и однажды они не возвращались после загрузки при
      полностью зелёной панели (`bash tests/router/verify.sh` проверяет это сам).
- [ ] **Переключение протоколов по кругу.** awg → reality → hysteria2 → awg, после каждого —
      `verify.sh`. Смена сервера в рамках одного протокола идёт другим методом (`replace_*_conf`).
- [ ] **Замена конфига.** `replace_awg_conf` — новый конфиг применяется, при неудачном
      handshake — автооткат к прежнему.
- [ ] **Обновление.** Повторный запуск bootstrap на настроенном роутере — апгрейд пакета,
      конфигурация и install-токен не трогаются (см. [[bootstrap#Обновление]]).
- [ ] **factory_reset.** Снимает конфигурацию cheburnet, роутер готов к повторной установке.

## После публикации тега

- [ ] Workflow `release` зелёный, в Release лежит ассет `cheburnet.apk` ожидаемого размера.
- [ ] `releases/latest/download/cheburnet.apk` отдаёт ИМЕННО новый пакет — по этой ссылке ходит
      bootstrap у всех пользователей. Проверять обязательно: однажды latest указывал на пакет
      прошлой архитектуры, и однострочник из README ставил не то, что мы тестировали.
- [ ] Bootstrap с дефолтными URL (без env-override) на чистом роутере доходит до мастера.

**Если хоть один пункт не прошёл — тег не публикуем.** Фиксируем баг, чиним, проверяем заново.

## Дальше

- [[reliability]] — пирамида тестов целиком
- [[bootstrap]] — установка и обновление
- [[hardware-requirements]] — на чём проверять
